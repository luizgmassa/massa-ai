# Validation — sensor-repair-2026-07 (PR-A)

- **Feature**: `sensor-repair-2026-07`
- **Branch**: `fix/sensor-repair` off `origin/main` @ `a6216cd` (v1.9.0) — not pushed, no PR
- **Commit range verified**: `origin/main..HEAD` (`39afe59` .. `fcf5a02`, 19 commits) plus the
  current uncommitted working tree (`.specs/features/sensor-repair-2026-07/{spec,design,tasks}.md`)
- **Verifier**: independent verification agent (author ≠ verifier); no source, test, or spec file
  was modified during this review
- **Date**: 2026-07-28 (session clock; environment date 2026-07-29 by wall clock)

---

## Requirement verdicts

| Req | Verdict | Evidence |
| --- | --- | --- |
| SEN-01 | **VERIFIED** | `scripts/check-coverage.ts:402` `isDedicatedDatabase()` is the single predicate used by both `assertDedicatedDatabase()` (:423-426) and `resetDedicatedDatabase()` (:501) — one condition, not two, as claimed. `TRUNCATION_EXCLUSIONS = ["_prisma_migrations"]` (:457), table list read from `pg_tables` at run time (:511-515), before/after row-count guard on the migrations table (:522-531). `architecture-map.test.ts` seam (`mock.module("../data/vector/vector-store-factory.js")`) present; budgets at 30_000 (grep confirms, not re-timed — see "could not verify"). |
| SEN-02 | **PARTIAL** | `.github/workflows/coverage.yml` exists, name `Coverage` (not `CI`), `on: push:[main], pull_request:[main]`, no `continue-on-error`, own `pgvector:pg17` service, `DATABASE_URL=postgresql://massa_ai:massa_ai_password@127.0.0.1:5433/massa_ai_test`, `MASSA_AI_DEDICATED=1`, `RUN_POSTGRES_TESTS=1` all literally present. `ci.yml` diff against `origin/main` is empty — coverage.yml does not extend the `workflow_run: [CI]` chain. `CLAUDE.md`'s CI-gates section is corrected (committed, matches file verbatim — confirmed by reading the section from this session's own system context). **Not verifiable**: AC-1's "green run on the PR itself" — no PR exists yet (task's own tasks.md flags this: "pending push"). |
| SEN-03 | **VERIFIED** | `scripts/lib/run-tests-isolated.ts:79-109` `buildChildEnv()`: explicit `MASSA_AI_TEST_CONFIG_HOME` wins over scratch (:85), `MASSA_AI_LLM_*` keys deleted unless `MASSA_AI_TEST_ALLOW_LLM=1` (:87-90), `MASSA_AI_LLM_ENABLED` re-pinned to `"false"` rather than left deleted (:105), matching the AC-6 divergence exactly (a deleted key gets refilled by `.env` autoload; an assigned one does not). `turbo.json` passthrough list gained `MASSA_AI_TEST_ALLOW_LLM`, `MASSA_AI_TEST_CONFIG_HOME`. Ran `scripts/__tests__/runner-child-env.test.ts` live: **10 pass / 0 fail**. |
| SEN-04 | **VERIFIED** | Fixture diff (`origin/main..HEAD`) shows every needle's `filePath/lineStart/lineEnd` replaced by `anchor/startOffset/endOffset`; no `query`/`rationale`-carrying content is invented, and the description records the `af3dab6` recovery for N07/N08/N09. `scoring.staleNeedles` is `[]` in the current fixture (grandfather list closed). Floor constants in `package.json` (`NEEDLE_FLOOR_HIT1`/`NEEDLE_FLOOR_MRR`) are untouched by the diff (file not in the changed-file list at all). `14.needles.test.ts`'s own floors (`HIT1_FLOOR=0.36`, `HIT5_FLOOR=0.64`, `MRR_FLOOR=0.47`) are **byte-identical** in `origin/main` and `HEAD` — pre-existing constants, not touched by this PR. Ran `scripts/__tests__/needle-resolution.test.ts` live: **18 pass / 0 fail**. |
| BEH-01 | **VERIFIED** | `MemoryController.search` forwards `includePersistent` into the `fullTextSearch` filters object (`memory-controller.ts:298-306`); previously it was destructured and discarded. `MemoryRepositoryPg.search`/`fullTextSearch` apply `level <> PERSISTENT` in SQL when `includePersistent === false`. Confirmed **no production caller** of `MemoryRepositoryPg.search(filters)` exists outside test files — only `memory-controller.test.ts`, `memory-repository-pg-coverage.test.ts`, `rlm-search.test.ts` reference `.search(` on a memory repo, and none of those is production code. MCP schema (`tools/search_memories.ts:61`) diff against `origin/main` is empty — unchanged, as required. Ran `memory-controller.test.ts` live: **32 pass / 0 fail**, matching the recorded post-fix count exactly. CHANGELOG entry present under `### Fixed`, states the default-unaffected property. |

### Discriminating-sensor spot checks (requirement B — 3+ required, reasoned from source, no revert performed)

1. **T6a** (`54 pass / 2 fail` reverted → `56/0` fixed): `declarationGroupKey(file, qualifiedName)` (`resolver.ts:190-192`) is kind-free. Mentally restoring `kind` to the key would put `class Same` and `interface Same` each alone in a group of size 1, both classified `unique`, both assigned the same simple FQN `file#Same` — which is exactly the collision the rewritten test at `structural-resolver.test.ts:968-999` now asserts does *not* happen (`overload` must be `["overloaded","overloaded"]`, `fqns` must be 2 distinct values, neither equal to `"src/main.ts#Same"`). A `kind`-scoped key fails that assertion directly. Ran the file live: **56 pass / 0 fail**, matching the "fixed" side of the claimed table exactly.
2. **T6b** (`2 pass / 4 fail` → `6/0`): `config/index.ts:832-833` gates `validateAllowedExtensionsConfig` behind `fileConfig.security?.allowedExtensions ? … : DEFAULT_ALLOWED_EXTENSIONS`. Reverting to a hardcoded `[...DEFAULT_ALLOWED_EXTENSIONS]` array (pre-`ee07326` shape, confirmed via `git show a6216cd:packages/shared/src/config/massa-ai-config.ts` — the type had no `allowedExtensions` field at all) removes both the override behavior *and* the validation call. Of the 6 tests in `allowed-extensions-config.test.ts`, exactly 4 assert validation-triggered outcomes (override applied, empty array refused, no-dot refused, non-array refused) — each fails under the revert — and 2 assert default-fallback outcomes that are true whether or not the code path runs. **2 pass / 4 fail** reproduces exactly.
3. **BEH-01 controller** (`30 pass / 2 fail` → `32/0`): reverting `includePersistent` to a discarded `_includePersistent` destructure removes the key from the `fullTextSearch(query, limit*3, {...})` call object entirely. Both new tests assert `toMatchObject({includePersistent: false | true})` against `mock.calls[0][2]` — `toMatchObject` fails when the key is absent, not merely mismatched, so both fail deterministically under the revert. Ran live: **32 pass / 0 fail** on the current (fixed) code.

Additionally confirmed empirically (not merely reasoned) that a one-element `glob` brace pattern (`**/*{.ts}`) matches **0** files while the array form (`buildExtensionGlob`) and a multi-element brace both match correctly — this is the mechanical basis of the T6d claim, reproduced with the actual `glob` package this repo depends on, not assumed.

---

## Unplanned work (T6a, T6b, T6c, T6d, e2e auth probe, coverage fix)

| Item | Commit | Verdict | Evidence |
| --- | --- | --- | --- |
| T6a — kind-free declaration group key | `57a75f8` | VERIFIED | See spot check 1 above. Blast-radius claim (`overload` not an input to `canonicalizeStructuralSignature`) checked: `fqn-codec.ts:127-136`'s listed signature inputs are version/language/dialect/qualifiedName/kind/arity/typeTokens/modifiers — `overload` is not among them, confirming the claim by inspection. |
| T6b — `security.allowedExtensions` propagates | `ee07326` | VERIFIED | See spot check 2. Empty-array rejection test present and passing; default-fallback (`omitting the key falls back to DEFAULT_ALLOWED_EXTENSIONS`, `no config.json at all still yields the defaults`) present, both pinning D's default-parity claim. |
| T6c — `capturePolicy` reaches discovery | `8ea1a4d` | VERIFIED | `discover-capture-policy.test.ts:90` `"no configured policy → discovery is unchanged"` is the default-parity test for D. Ran live: **10 pass / 0 fail**. `includeTests`-survives-the-policy-layer test present, matching the claimed naive-wiring defect the existing suite caught. |
| T6d — one-extension glob | `c3f10ec` | VERIFIED | `buildExtensionGlob` (`ignore-patterns.ts:43-45`) returns one `**/*.ext` pattern per extension; empirically confirmed the `glob` package treats a single-alternative `{…}` as literal text (0 matches) while an array of patterns or a multi-alternative brace both match correctly. Applied at all three call sites claimed (`discover.ts:85-94`, `index-manager.ts:254-262`, `rlm-indexing.ts:143-146`) — confirmed by diff. |
| e2e auth probe | `fedc202` | VERIFIED | `_helpers.ts` diff adds `headers: apiHeaders()` to exactly the two previously-keyless fetches (`/api/v1/system/ollama`, `/api/v1/system/info`), matching AD-011's mandatory-auth requirement and the claimed `0 pass/2 skip` → `1 pass/0 fail` mechanism. |
| Coverage-floor fix for two config validators | `fcf5a02` | VERIFIED | Exports two previously-unexported validators and adds `config-validators.test.ts` (166 lines, in-process). Change to `config/index.ts` is exactly `function` → `export function` on two declarations — no logic touched, consistent with a coverage-only fix. |

---

## Claims I could not verify (and why)

- **Full `bun run test`, `test:scripts`, `test:plugins`, and `bun run test:coverage` (×2, dedicated DB)** — explicitly prohibited by the task's hard constraints (concurrent-run corruption risk, 1–45 min each). Relied on the authoring session's recorded output plus my own narrow spot-checks (7 individual test files run directly, all matching recorded counts exactly: `needle-resolution.test.ts` 18/0, `structural-resolver.test.ts` 56/0, `runner-child-env.test.ts` 10/0, `check-coverage.test.ts` 22/0, `memory-controller.test.ts` 32/0, `ignore-patterns.test.ts` 19/0, `discover-capture-policy.test.ts` 10/0 — all green, zero discrepancies found).
- **Core/tools-api/mcp-client isolated-group counts (126/25/8)** — reproducing these requires running the actual isolation runner, which has no discovery-only/dry-run mode and would execute the full suite. Not re-run.
- **`embedded-api-client-endpoints.test.ts` green ×3 consecutively** — same reason; not re-run.
- **SEN-02 AC-1, coverage.yml green on a real PR** — cannot exist yet; branch is unpushed with no PR. Recorded correctly in tasks.md as "pending push."
- **T3's exact timing numbers (895 ms / 30_000 budget headroom)** — did not re-run `test:coverage` to re-time; confirmed only that the seam (`mock.module(...vector-store-factory.js)`) and the `30_000` budget constant are present in the file as claimed.
- **T7's needles-gate wall-clock numbers (hit@1/MRR before/after T5a/T5b)** — did not re-run `bun run bench:needles:gate` (requires local Ollama + qwen3-embedding:8b, real cost even at ~2 min/run × several runs); relied on the recorded JSON report filenames existing on disk (not opened/verified byte-for-byte) and the internal consistency of the numbers with the fixture diff.

---

## Discrepancies found

1. **A material slice of documentation is uncommitted.** `git status` shows `.specs/features/sensor-repair-2026-07/{spec.md,design.md,tasks.md}` modified in the working tree with no commit behind them. Concretely, the **committed** `HEAD` (`fcf5a02`) versions currently differ from the tree state a reviewer would see if this branch were pushed today:
   - `spec.md` at `HEAD` still reads *"PR-A carries two behavior changes, not one"* (T6a only) — the correction to **four** (adding T6b/T6c/T6d) exists only in the working tree.
   - `design.md` at `HEAD` ends at the Fourth fork (T6a). The entire Fifth fork — the three inert-knob findings (T6b/T6c/T6d) and, critically, **the honesty caveat that T6's gate ran against a bounded 382-file corpus and is weaker evidence than the full-corpus calibration** — exists only in the working tree, not in any commit.
   - `tasks.md` at `HEAD` has T6's own "Done when" boxes still unchecked (`[ ]`) even though the commits implementing T6 (`fedc202`, `d5b5813`) predate `fcf5a02` in the log, and has no T6a/T6b/T6c/T6d sections at all.
   - `.specs/project/STATE.md` is likewise uncommitted for this: the committed version still shows `AD-013 | proposed … not yet implemented`, and the *"Six unplanned repairs"* summary is working-tree-only.
   This does not falsify any technical claim — I found the honesty caveat and the corrected count exactly where the task asked me to look, on disk — but it means the record that would actually ship in a PR right now is stale relative to the code, and specifically **omits** the bounded-corpus caveat and the corrected behavior-change count from every committed artifact.
2. **`.specs/project/FEATURES.json` is committed but stale.** It marks `sensor-repair-2026-07` as `phases: {specify: true, design: false, tasks: true, execute: false}`, `status: "specified"` — despite 8 of 9 planned tasks plus 6 unplanned repairs being executed and committed. No commit in the range touches this file's phase flags for this feature.
3. **T9's own "Done when" checklist is honestly incomplete**, not falsely marked: `CHANGELOG.md` entries (present, but box unchecked), full gate green (not re-run this session, box unchecked), "no new exclusion" (verified true — count is 9 — but box unchecked). These are consistent with T9 genuinely not being closed, which is itself accurate self-reporting, not a discrepancy against the tree — flagged here only so it isn't mistaken for a hidden gap.
4. **Minor citation slip**: T7's task entry names its closing commit as `docs(specs): record the needle-anchoring equivalence baseline`, but no commit with that exact subject exists in `origin/main..HEAD`; the T7 content appears to have landed folded into one of the `docs(specs):` commits earlier in the range (`eabf02c`, `1dc59c8`, or `5e7d54d`). Not a substantive problem — the content is present at `HEAD` in `tasks.md` — just an inaccurate commit citation.

No other discrepancies found. Checked hard enough to be confident on: all four "inert knob" claims in design.md's Fifth fork (verified against `a6216cd` source directly, plus one empirical `glob` library test); the needle fixture diff end-to-end (no query, no floor, no filePath/anchor content invented beyond the documented `af3dab6` recovery); the CHANGELOG's coverage of all four behavior changes including the empty-array-rejection and omit-key-is-unaffected properties; the `EXCLUSIONS` array in `check-coverage.ts` (counted programmatically — exactly 9, matching both the gate's own log line and this PR's "no new exclusion" constraint); and three independently-reasoned discriminating sensors plus one empirically-reproduced glob-library behavior, all reproducing the claimed counts exactly.

---

## Residual risk

- **The bounded-corpus caveat (T6's e2e needles run using only a 382-file `.ts`-only index) is real and correctly stated in the working tree, but not committed anywhere.** If PR-A is opened from the current commit history without an additional documentation commit, reviewers will not see this caveat, the four-behavior-change count, or the AD-013/FEATURES.json status update.
- **No fresh, integrated `test:coverage` ×2 run has been recorded against the *final* tree** (i.e., after T6a/b/c/d, the auth-probe fix, and the coverage-validator fix all landed together). T3's own discipline — "once is not evidence for a budget that already flapped" — was applied to the architecture-map seam in isolation, but the last full-gate confirmation predates `fcf5a02` (the last commit, itself a reaction to a floor regression `ee07326` caused). This session did not re-run the full gate (prohibited), so whether the fully-integrated tree still clears 90% everywhere is not independently confirmed post-`fcf5a02`.
- **SEN-02's actual CI behavior is unproven** — the workflow is well-formed by static reading and one negative-case predicate check, but has never executed in GitHub Actions.
- **Branch is unpushed, no PR exists.** All of the above documentation gaps are trivially fixable with one more commit before opening the PR; none require touching source or tests.
