# ============================================
# massa-ai - Docker Compose
# ============================================
# Services:
#   postgres - PostgreSQL database (port 5432)
#   api      - REST API (port 3333)
#   mcp      - MCP stdio client (connects to api)
#
# Usage:
#   docker compose up -d postgres       # PostgreSQL only
#   docker compose up -d api            # API + PostgreSQL
#   docker compose up -d                # All services
#   docker compose logs -f              # Watch logs
#
# MCP via Docker (Claude Desktop / OpenCode):
#   docker compose run --rm -i mcp
#
# Build locally instead of pulling from DockerHub:
#   docker compose up -d --build
# ============================================

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: massa-ai-postgres
    restart: unless-stopped
    ports:
      - "${MASSA_AI_POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: massa_ai
      # Set POSTGRES_PASSWORD in .env before production deploys
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-massa_ai_password}
      POSTGRES_DB: massa_ai
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U massa_ai"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    image: ${MASSA_AI_API_IMAGE:-massa/massa-ai:api-latest}
    container_name: massa-ai-api
    restart: unless-stopped
    ports:
      - "${MASSA_AI_API_PORT:-3333}:3333"
    volumes:
      - massa-ai-data:/data
    environment:
      - NODE_ENV=production
      - MASSA_AI_API_PORT=3333
      # Optional. Left unset, the API generates a key on first start and saves
      # it to security.apiKey inside the mounted volume
      # (/data/massa-ai/config.json) — read it from there. Set it to pin a
      # specific value; an explicit env var always wins over the stored one.
      - MASSA_AI_API_KEY=${MASSA_AI_API_KEY:-}
      - DATABASE_URL=postgresql://massa_ai:${POSTGRES_PASSWORD:-massa_ai_password}@postgres:5432/massa_ai
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      - OLLAMA_EMBEDDING_MODEL=${OLLAMA_EMBEDDING_MODEL:-qwen3-embedding:8b}
      - OLLAMA_EMBEDDING_DIMENSIONS=${OLLAMA_EMBEDDING_DIMENSIONS:-4096}
      - RLM_LLM_ENABLED=${RLM_LLM_ENABLED:-false}
      - RLM_LLM_API_KEY=${RLM_LLM_API_KEY:-}
      - RLM_LLM_MODEL=${RLM_LLM_MODEL:-}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3333/health"]
      interval: 30s
      timeout: 3s
      start_period: 60s
      retries: 3

  mcp:
    image: ${MASSA_AI_MCP_IMAGE:-massa/massa-ai:mcp-latest}
    container_name: massa-ai-mcp
    stdin_open: true
    # Shares the API's volume so the MCP client can read the key the API
    # auto-provisioned into /data/massa-ai/config.json. Without it this
    # container has no way to learn the generated key and every call it makes
    # to the API returns 401 — `depends_on: service_healthy` guarantees the API
    # has already provisioned by the time this starts.
    volumes:
      - massa-ai-data:/data
    environment:
      - NODE_ENV=production
      - MASSA_AI_API_URL=http://api:3333
      # Same contract as the api service: unset means "read it from the shared
      # config.json", set means "pin this value".
      - MASSA_AI_API_KEY=${MASSA_AI_API_KEY:-}
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres-data:
    driver: local
  massa-ai-data:
    driver: local
