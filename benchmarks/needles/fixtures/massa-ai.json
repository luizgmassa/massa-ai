{
  "projectId": "massa-ai",
  "version": "1.3.0",
  "description": "Needles validated against the massa-ai codebase itself (dogfooding). Each needle targets a non-obvious heuristic, magic constant, or scoring rule that pure keyword search would miss. Targets are identified by CONTENT (anchor + signed line offsets), not by physical position — see benchmarks/needles/resolve.ts. Anchors authored 2026-07-28; the 11 anchored spans reproduce the positional values they replaced byte-exactly.",
  "scoring": {
    "topK": 10,
    "hitAtK": [
      1,
      3,
      5,
      10
    ],
    "lineTolerance": 5,
    "notes": "A hit requires the returned chunk's filePath to equal the needle's RESOLVED filePath AND [lineStart, lineEnd] to intersect the resolved range (with ±lineTolerance on each side).",
    "staleNeedlesNote": "Needle ids permitted to carry a legacy positional target instead of an anchor. These three point into smart-chunker.ts at lines 198-206 / 642-674 / 737-744, but that file was split into services/search/chunker/ at 56c84d1 and now ends at line 81 — the fixture was authored at af3dab6, the commit before. They are carried unchanged for exactly one commit so the anchoring change can be proven rank-for-rank equivalent on an unchanged corpus before the repair moves the score. The list must shrink to empty: an id listed here that also carries an anchor is a hard failure, so the entry cannot outlive its reason.",
    "staleNeedles": [
      "N07-brace-counter-strip-strings",
      "N08-no-merge-labeled-chunks",
      "N09-markdown-no-headings-fallback"
    ]
  },
  "needles": [
    {
      "id": "N01-pagerank-damping",
      "category": "string-literal-config-magic",
      "difficulty": "medium",
      "query": "constante de damping usada no algoritmo iterativo de relevância de arquivos",
      "expected": {
        "anchor": "const DAMPING = 0.85;",
        "startOffset": 0,
        "endOffset": 0
      },
      "rationale": "DAMPING=0.85 declarado como constante top-level. Sem `pagerank` no nome, exige conectar 'damping' ao conceito de PageRank."
    },
    {
      "id": "N02-pagerank-iterations",
      "category": "string-literal-config-magic",
      "difficulty": "medium",
      "query": "limite máximo de iterações no cálculo de centralidade",
      "expected": {
        "anchor": "const ITERATIONS = 20;",
        "startOffset": 0,
        "endOffset": 0
      },
      "rationale": "ITERATIONS=20 sem ligação textual a centralidade ou PageRank."
    },
    {
      "id": "N03-keyword-boost-code-query",
      "category": "business-rule-in-utility",
      "difficulty": "hard",
      "query": "fator multiplicador aplicado a resultados de keyword search quando a query parece código",
      "expected": {
        "anchor": "const KEYWORD_BOOST = isCodeQuery ? codeKeywordBoost : 1.0;",
        "startOffset": -8,
        "endOffset": 1
      },
      "rationale": "isCodeQuery + KEYWORD_BOOST (RRF_KEYWORD_BOOST env, default 2.5). Heurística de boost para code queries. Anchor na própria linha de KEYWORD_BOOST: o lineStart original (57) é um comentário e o lineEnd (66) é uma linha em branco, nenhum dos dois ancorável."
    },
    {
      "id": "N04-rrf-vector-blend",
      "category": "domain-concept-without-keyword",
      "difficulty": "hard",
      "query": "proporção entre score RRF e similaridade vetorial bruta na composição do score final",
      "expected": {
        "anchor": "const rrfNormalized = rrfScore / maxRrfScore;",
        "startOffset": 0,
        "endOffset": 5
      },
      "rationale": "vectorWeight (RRF_VECTOR_WEIGHT env, default 0.3) → combinedScore = rrfNormalized*(1-w) + vectorSimilarity*w. Split 70/30 sem constantes nomeadas."
    },
    {
      "id": "N05-centrality-rerank-bonus",
      "category": "domain-concept-without-keyword",
      "difficulty": "hard",
      "query": "bônus aplicado no ranking final para arquivos com alta centralidade no graph",
      "expected": {
        "anchor": "const normalizedScore = Math.min(1, combinedScore * (1 + 0.2 * centralityScore));",
        "startOffset": -7,
        "endOffset": 0
      },
      "rationale": "normalizedScore = combinedScore * (1 + 0.2 * centralityScore). Multiplicador 0.2 nu, sem constante. Anchor no fim do span: o lineStart original (177) é um comentário."
    },
    {
      "id": "N06-minscore-on-raw-vector",
      "category": "business-rule-in-utility",
      "difficulty": "hard",
      "query": "filtro de score mínimo aplicado na similaridade vetorial bruta e não no score normalizado por RRF",
      "expected": {
        "anchor": "rerankedTop = applyProximityRerank(rerankInput, query);",
        "startOffset": -14,
        "endOffset": 0
      },
      "rationale": "Decisão crítica: minScore usa _rrfRawVectorScore quando disponível porque RRF normaliza top=1.0. Anchor no fim do span: o lineStart original (340) é `    }`, que ocorre 11130 vezes no repo."
    },
    {
      "id": "N07-brace-counter-strip-strings",
      "category": "business-rule-in-utility",
      "difficulty": "hard",
      "query": "lógica que ignora chaves dentro de strings, regex ou comentários ao contar profundidade de bloco",
      "expected": {
        "filePath": "packages/core/src/services/search/smart-chunker.ts",
        "lineStart": 642,
        "lineEnd": 674
      },
      "rationale": "netBraceDelta strip prévio. Crítico para arquivos com regex literais (parsers, validadores) — drift de depth quebra chunking. STALE: alvo aponta para além do EOF de smart-chunker.ts (81 linhas) desde o split em 56c84d1; recuperado e re-ancorado no passo seguinte."
    },
    {
      "id": "N08-no-merge-labeled-chunks",
      "category": "business-rule-in-utility",
      "difficulty": "medium",
      "query": "regra que impede que chunks de código com rótulo semântico sejam mesclados com vizinhos pequenos",
      "expected": {
        "filePath": "packages/core/src/services/search/smart-chunker.ts",
        "lineStart": 737,
        "lineEnd": 744
      },
      "rationale": "Métodos curtos (3-4L getter/setter) têm label = não devem ser absorvidos pelo merge anti-fragmentação. STALE: ver N07."
    },
    {
      "id": "N09-markdown-no-headings-fallback",
      "category": "business-rule-in-utility",
      "difficulty": "medium",
      "query": "fallback que usa chunking fixo quando arquivo markdown não tem nenhum heading",
      "expected": {
        "filePath": "packages/core/src/services/search/smart-chunker.ts",
        "lineStart": 198,
        "lineEnd": 206
      },
      "rationale": "Sem essa regra, README sem ## vira 1 mega-chunk que matcha qualquer query (bias .md descoberto no benchmark Sicad). STALE: ver N07."
    },
    {
      "id": "N10-discover-sort-priority",
      "category": "domain-concept-without-keyword",
      "difficulty": "medium",
      "query": "ordem de prioridade dos arquivos durante a fase de discover do ETL",
      "expected": {
        "anchor": "batch.map((rel) => this.processFile(ctx, rel, opts.forceReindex ?? false)),",
        "startOffset": -1,
        "endOffset": 6
      },
      "rationale": "Ordem: centrality desc → mtime desc → path asc. Garante que arquivos importantes são embebidos primeiro (early-stop friendly). Anchor interno: `const results = await Promise.all(` ocorre 4× e `ctx.emit({` 16×."
    },
    {
      "id": "N11-fingerprint-cache-key",
      "category": "business-rule-in-utility",
      "difficulty": "medium",
      "query": "comparação de hash que decide se um arquivo precisa ser reparseado no incremental reindex",
      "expected": {
        "anchor": "return discovered;",
        "startOffset": -6,
        "endOffset": 0
      },
      "rationale": "needsReparse = stored.content_hash !== contentHash. Skip por SHA-256 é o que torna reindex incremental viável. Anchor no fim do span: `projectId: ctx.projectId,` ocorre 11×."
    },
    {
      "id": "N12-vector-table-by-dimension",
      "category": "cross-language-stack",
      "difficulty": "medium",
      "query": "roteamento dinâmico para tabela do postgres baseado na dimensão do embedding",
      "expected": {
        "anchor": "return `vector_documents_${dimensions}d`;",
        "startOffset": -3,
        "endOffset": 0
      },
      "rationale": "getTableName retorna `vector_documents_${dim}d`. Conecta env var → provider config → schema postgres. Anchor no fim do span: o lineStart original (60) é um comentário separador."
    },
    {
      "id": "N13-orphaned-chunks-warning",
      "category": "cross-file-event-coupling",
      "difficulty": "hard",
      "query": "alerta emitido quando troca de modelo de embedding deixa chunks de outro tamanho dimensional órfãos",
      "expected": {
        "anchor": "GROUP BY project_id`,",
        "startOffset": -44,
        "endOffset": 0
      },
      "rationale": "detectOrphanedChunks varre tabelas vector_documents_*d e avisa se houver dados em dim antiga. Bug silencioso comum. Anchor no fim do span: o lineStart original (137) é uma linha em branco."
    },
    {
      "id": "N14-binary-quantization-sign",
      "category": "string-literal-config-magic",
      "difficulty": "medium",
      "query": "regra de conversão de embedding float em bit-string usando o sinal do valor",
      "expected": {
        "anchor": "private normalizeScore(raw: unknown): number {",
        "startOffset": -7,
        "endOffset": 0
      },
      "rationale": "floatsToBit: x ≥ 0 → '1', senão '0'. Threshold 0 hardcoded; base do binary quantization (≥ 2000d). Anchor no fim do span: o lineStart original (67) é um comentário de JSDoc."
    }
  ]
}
